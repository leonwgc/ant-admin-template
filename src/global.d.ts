/**
 * Whether the current environment is in mock mode.
 * It is set to `true` in development & MOCK mode and `false` in production mode.
 * You can use it to judge whether to use mock data or not.
 */
declare const MOCK: boolean;
